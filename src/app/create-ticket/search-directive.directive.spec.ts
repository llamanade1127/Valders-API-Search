import { SearchDirectiveDirective } from './search-directive.directive';

describe('SearchDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new SearchDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
